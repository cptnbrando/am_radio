import { DomWatcherDirective } from './dom-watcher.directive';

describe('DomWatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new DomWatcherDirective();
    expect(directive).toBeTruthy();
  });
});
